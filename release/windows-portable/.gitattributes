# Windows scripts/text must ship with CRLF line endings inside the zip,
# even when the release is assembled on a Linux CI runner.
*.bat       text eol=crlf
*.txt       text eol=crlf
*.pac       text eol=crlf
*.vbs       text eol=crlf
proxy.conf  text eol=lf
